---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/components/Auth.jsx
  - src/components/Onboarding.jsx
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05]
must_haves:
  truths:
    - "User can sign up with email/password and the session persists across a page refresh"
    - "User can sign in with email/password and is recognized after refresh"
    - "User can tap Enter as Guest and reach onboarding in one click with an anonymous profile created in the active store"
    - "A new user completes the 4-step onboarding (name, target exam, exam date, plant seed) and the profile is written via dbService.updateUser"
  artifacts:
    - path: "src/components/Auth.jsx"
      provides: "Glassmorphic login/signup card with Sign Up / Sign In tabs, validation, and one-click guest entry — dual-path for live and mock mode"
      contains: "Enter as Guest"
      min_lines: 90
    - path: "src/components/Onboarding.jsx"
      provides: "4-step Zen Gate form creating the user document and signalling completion to App"
      contains: "Continue Setup"
      min_lines: 90
  key_links:
    - from: "src/components/Auth.jsx"
      to: "firebase/auth"
      via: "createUserWithEmailAndPassword / signInWithEmailAndPassword / signInAnonymously in live mode"
      pattern: "signInAnonymously|createUserWithEmailAndPassword"
    - from: "src/components/Onboarding.jsx"
      to: "src/services/dbService.js"
      via: "updateUser writes the completed profile"
      pattern: "updateUser"
    - from: "src/components/Auth.jsx"
      to: "src/services/firebase.js"
      via: "branches on MOCK_MODE for guest + email auth"
      pattern: "MOCK_MODE"
---

<objective>
Build the auth entry flow (TRACK B components): `Auth.jsx` (signup/login/guest) and `Onboarding.jsx` (the 4-step Zen Gate). Both run dual-path — Firebase Auth in live mode, localStorage-backed sessions in mock mode — and consume the design system (plan 01-01) and service layer (plan 01-02).

Purpose: Delivers AUTH-01 through AUTH-05 and underpins ROADMAP success criteria 1 (signup + persisted login), 2 (one-click guest), and 3 (onboarding writes profile and routes to dashboard). These components expose callbacks that App.jsx (plan 01-05) wires into the state machine.
Output: Two components rendering the exact UI-SPEC surfaces and copywriting, with validation and dual-path auth.
</objective>

<execution_context>
@/Users/aquibvadsaria/project/playground/Mental-Wellness-Tracker/.claude/gsd-core/workflows/execute-plan.md
@/Users/aquibvadsaria/project/playground/Mental-Wellness-Tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@DESIGN.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<interface_context>
Consumes the contracts produced by plan 01-01 (CSS) and plan 01-02 (services):
- From src/index.css: `.glass-card`, `.btn-primary`, `.btn-secondary`, all `--*` tokens, `breathe` keyframe (plant cards).
- From src/services/firebase.js: `auth`, `MOCK_MODE`.
- From src/services/dbService.js: `getUser`, `updateUser`.
- From firebase/auth (live mode only): `createUserWithEmailAndPassword`, `signInWithEmailAndPassword`, `signInAnonymously`.
- From lucide-react: icon components as needed (e.g., Mail, Lock, User).

Props this plan EXPORTS for the App.jsx wiring (plan 01-05):
- `Auth({ onAuth })` — calls `onAuth(user, profile)` after successful signup/login/guest entry; in mock mode it also writes `zenstudy:mock:user` to localStorage so the session survives refresh.
- `Onboarding({ user, onComplete })` — calls `onComplete(profile)` after the final step persists the profile via `updateUser`.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Auth.jsx — signup, login (persisted), and one-click guest, dual-path</name>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (Surface 1: Auth Screen — layout, states, tabs, fields, buttons, error/loading; Copywriting Contract → Auth Screen, all exact strings; Accessibility Contract → role="alert" errors, focus, 44px targets)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4 guest entry; Pattern 6 session persistence; Pitfall 1 loading state; auth error→message mapping needs; Anti-Patterns: never read auth.currentUser on load)
    - DESIGN.md §2.1 (border shifts purple→teal between signup/login; Firebase calls)
  </read_first>
  <files>src/components/Auth.jsx</files>
  <behavior>
    - Default render: Sign Up tab active, card border glows --accent-violet; headline "Welcome to ZenStudy", subline "Your calm space for exam prep."
    - Switching to Sign In tab: border transitions to --accent-teal; headline "Welcome back", subline "Continue your wellness journey."; Name field hidden.
    - Submitting empty email shows "Email is required." with role="alert"; invalid email shows "Enter a valid email address."; password < 6 chars shows "Password must be at least 6 characters."
    - Live mode wrong credentials surface "Email or password is incorrect."; existing email on signup surfaces "An account with this email already exists. Sign in instead."; other failures surface "Something went wrong. Please try again."
    - Clicking "Enter as Guest" reaches onboarding in one action (no extra form), creating an anonymous session.
    - During an in-flight request the CTA is disabled and its label is "…".
  </behavior>
  <action>
    Create src/components/Auth.jsx as a function component accepting { onAuth }. Render the UI-SPEC Surface 1 exactly: a centered .glass-card (max-width 420px) on a --bg-deep full-viewport background, "Sign Up" / "Sign In" tabs that conditionally render fields (Name input only on Sign Up), and inputs styled per the UI-SPEC field spec via class names only (zero inline styles — UI-SPEC constraint). Use every Auth-Screen copywriting string verbatim. Apply the violet(signup)→teal(login) border shift via a CSS class toggle with transition (DESIGN.md §2.1), not animated backdrop-filter.

    Implement client-side validation producing the exact error strings; render errors inline below the triggering field as Inter 14px in --color-destructive with role="alert" (Accessibility Contract). Use semantic &lt;form&gt;, &lt;label&gt;, &lt;input&gt;, &lt;button&gt;; all interactive targets ≥44px via the design-system classes.

    Dual-path auth branching on MOCK_MODE (imported from firebase.js):
    - Mock mode signup/login: generate or look up a local uid, write { uid, isAnonymous:false, email } to localStorage key `zenstudy:mock:user` (try/catch), then read profile via getUser and call onAuth(user, profile). This gives AUTH-02 persistence in mock mode (the key survives refresh).
    - Live mode: call createUserWithEmailAndPassword / signInWithEmailAndPassword from firebase/auth. Do NOT call setPersistence — browserLocalPersistence is the web default and satisfies AUTH-02 (RESEARCH Pattern 6). Do NOT navigate manually; for live mode let onAuthStateChanged in App.jsx drive the transition (RESEARCH Pattern 4 / Anti-Patterns) — invoke onAuth only in mock mode where there is no observer.
    - Guest (AUTH-03), one-click: mock → write { uid: `guest_${Date.now()}`, isAnonymous:true } to `zenstudy:mock:user` then onAuth; live → signInAnonymously(auth) and let the App observer route. Keep this path frictionless per CONTEXT Specific Ideas. Map Firebase error codes (auth/wrong-password, auth/user-not-found, auth/email-already-in-use, auth/too-many-requests, default) to the exact copywriting strings — do not leak raw Firebase error text (Security threat model). Manage a loading boolean that disables the CTA and shows "…".
  </action>
  <verify>
    <automated>grep -q "Enter as Guest" src/components/Auth.jsx &amp;&amp; grep -q "Welcome to ZenStudy" src/components/Auth.jsx &amp;&amp; grep -q "Password must be at least 6 characters." src/components/Auth.jsx &amp;&amp; grep -q "signInAnonymously" src/components/Auth.jsx &amp;&amp; grep -q "MOCK_MODE" src/components/Auth.jsx &amp;&amp; grep -Lq "dangerouslySetInnerHTML" src/components/Auth.jsx &amp;&amp; npm run build 2>&amp;1 | tail -3</automated>
    <human-check>In mock mode: sign up with email/password, refresh the page, confirm the session is retained (no bounce to Auth). Click "Enter as Guest" and confirm onboarding appears in one click. Trigger each validation error and confirm exact copy + role="alert".</human-check>
  </verify>
  <acceptance_criteria>
    - All Auth-Screen copywriting strings appear verbatim; errors render with role="alert".
    - Mock signup/login persists the session across refresh via `zenstudy:mock:user`; live mode relies on Firebase default persistence (no setPersistence call).
    - "Enter as Guest" creates an anonymous session and reaches onboarding in one click (AUTH-03).
    - Firebase error codes are mapped to the curated strings; raw Firebase messages are not shown; no dangerouslySetInnerHTML; zero inline styles.
  </acceptance_criteria>
  <done>Auth.jsx delivers AUTH-01/02/03 in both live and mock modes, matching the UI-SPEC surface and copy, exposing onAuth(user, profile) for the App state machine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Onboarding.jsx — 4-step Zen Gate that creates the user profile (AUTH-04/05)</name>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (Surface 2: Onboarding — 4 steps, step indicator, exam pills, plant cards w/ breathe, Continue/Back, step transition; Copywriting Contract → Onboarding, exact strings incl. Step 4 template; Accessibility → aria-live errors)
    - DESIGN.md §2.2 (Zen Gate steps and plant seeds Cactus/Bonsai/Fern; trigger: creating user doc unlocks app)
    - DESIGN.md §3.1 (users schema fields to write: name, targetExam, targetDate, startingPlant, plus initial streak 0, zenPoints 0, createdAt, lastActive)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-08 seed only current user's profile)
  </read_first>
  <files>src/components/Onboarding.jsx</files>
  <behavior>
    - Step 1 requires a non-empty name and one selected exam (JEE/NEET/UPSC/Boards); Continue is disabled until both are set; submitting empty shows "Please enter your name." / "Choose an exam to continue."
    - Step 2 requires a date; missing date shows "Please pick your exam date."
    - Step 3 requires a selected plant (Cactus/Bonsai/Fern); missing shows "Choose a plant to grow with you."; each plant card's SVG runs the breathe keyframe.
    - Step 4 shows the summary "Hi [name], your [exam] journey starts now. Your [plant] is waiting." and a "Begin Your Journey" CTA.
    - Completing Step 4 calls updateUser with the assembled profile and then onComplete(profile).
  </behavior>
  <action>
    Create src/components/Onboarding.jsx as a function component accepting { user, onComplete }. Render UI-SPEC Surface 2 exactly: centered .glass-card (max-width 480px), a 4-dot step indicator (active dot --accent-violet), and the four steps. Step 1: name input + four exam pill buttons (selected state uses the --accent-violet pill style from UI-SPEC). Step 2: native &lt;input type="date"&gt; styled via class names. Step 3: three plant cards (Cactus/Bonsai/Fern) each 120×140 .glass-card with a simple inline SVG placeholder (real plant SVG is Phase 2) running the breathe animation; selected card gets the violet border + glow. Step 4: read-only summary using the exact template string.

    Use all Onboarding copywriting strings verbatim. "Continue Setup" CTA on Steps 1–3 (full-width .btn-primary, disabled until the step's required fields are valid); "Begin Your Journey" on Step 4; "Back" link top-left on Steps 2–4 (44px tappable). Render validation errors in an aria-live="polite" region with the exact error strings (Accessibility Contract). Animate step transitions via opacity/transform only (never backdrop-filter) inside the same card container.

    On final submit, assemble the profile per DESIGN.md §3.1: { name, targetExam, targetDate (ISO string from the date input), startingPlant (lowercased plant id), streak: 0, zenPoints: 0, redeemedItems: [], createdAt: now ISO, lastActive: now ISO }. Call dbService.updateUser(user.uid, profile) — this is the "creating the user document" trigger (AUTH-05, DESIGN.md §2.2). On success call onComplete(profile) so App routes to the dashboard. Seed ONLY the current user's profile (D-08) — no cohort/league/journal seeding. Validate inputs with plain JS (RESEARCH Security Domain V5 — name non-empty, exam in enum, date valid, plant in enum); no external validation library. Zero inline styles; no dangerouslySetInnerHTML when rendering the name/exam into the summary (React text nodes only).
  </action>
  <verify>
    <automated>grep -q "Continue Setup" src/components/Onboarding.jsx &amp;&amp; grep -q "Begin Your Journey" src/components/Onboarding.jsx &amp;&amp; grep -q "Choose your companion plant" src/components/Onboarding.jsx &amp;&amp; grep -q "updateUser" src/components/Onboarding.jsx &amp;&amp; grep -q "breathe" src/components/Onboarding.jsx &amp;&amp; grep -Lq "dangerouslySetInnerHTML" src/components/Onboarding.jsx &amp;&amp; npm run build 2>&amp;1 | tail -3</automated>
    <human-check>Complete all four steps (name, exam, date, plant); confirm the Step 4 summary renders the chosen values, "Begin Your Journey" persists the profile (visible under localStorage key zenstudy:users:{uid} in mock mode), and onComplete fires. Confirm each step's validation copy and that plant cards visibly breathe.</human-check>
  </verify>
  <acceptance_criteria>
    - All four steps render with exact UI-SPEC copy; the step indicator reflects progress; plant cards run the breathe keyframe.
    - Continue is gated by per-step validation with the exact error strings in an aria-live region.
    - Final submit writes the DESIGN.md §3.1 profile via updateUser (streak/zenPoints initialized to 0) and calls onComplete (AUTH-04/05).
    - Only the current user's profile is seeded; no inline styles; name/exam rendered as React text nodes.
  </acceptance_criteria>
  <done>Onboarding.jsx delivers AUTH-04/05: the 4-step Zen Gate creates the user document and signals completion so App can route to the dashboard, matching the UI-SPEC surface and copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (email/password/name/date) → app | Untrusted form input crosses into auth + profile writes |
| app → Firebase Auth | Credentials validated server-side by Firebase in live mode |
| Firebase error responses → UI | Raw error text could aid account enumeration if surfaced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Information Disclosure | Auth.jsx error messaging | mitigate | Map Firebase error codes to curated strings (UI-SPEC copywriting); never render raw Firebase error text — keeps enumeration at Firebase's default behavior and no worse. |
| T-01-10 | Tampering / XSS | Onboarding summary + Auth labels | mitigate | Render all user-supplied data (name, exam) as React text nodes; no dangerouslySetInnerHTML (CLAUDE.md / IMPLEMENTATION_PLAN.md). |
| T-01-11 | Input Validation | Onboarding form fields | mitigate | Plain-JS validation (ASVS L1 V5): name non-empty, exam in enum, date valid, plant in enum, before any write. |
| T-01-12 | Spoofing / Session | mock-mode session key | mitigate | `zenstudy:mock:user` stores only a uid + isAnonymous flag (no secret); wrapped in try/catch; mirrors Firebase browserLocalPersistence semantics for refresh survival. |
| T-01-13 | Denial of Service | anonymous guest (live) | accept | Default mock mode bypasses Firebase for examiners (Pitfall 5); live-mode anonymous rate limiting is Firebase-managed. |
</threat_model>

<verification>
- `npm run build` exits 0 with both components present.
- grep gates confirm verbatim copywriting and the auth/onboarding wiring identifiers.
- Mock-mode manual flow: signup persists across refresh; guest reaches onboarding in one click; onboarding writes the profile and fires onComplete.
- No dangerouslySetInnerHTML and no inline styles in either component.
</verification>

<success_criteria>
- AUTH-01/02/03 satisfied by Auth.jsx (signup, persisted login, one-click guest) in both modes.
- AUTH-04/05 satisfied by Onboarding.jsx (4-step capture → profile written → completion signalled).
- Components match the UI-SPEC surfaces, copy, accessibility, and security constraints exactly.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done.
</output>
