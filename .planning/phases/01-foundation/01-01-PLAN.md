---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - index.html
  - vite.config.js
  - .gitignore
  - .env.example
  - src/index.css
  - src/main.jsx
  - src/App.jsx
autonomous: true
requirements: [AUTH-06]
must_haves:
  truths:
    - "App boots with `npm install && npm run dev` and serves a page at http://localhost:5173 with no credentials configured"
    - "The glassmorphic design system (HSL tokens, keyframe animations, Outfit/Inter fonts) is available globally via src/index.css"
    - "No Firebase or Gemini API keys are committed; .env* is gitignored and .env.example documents the variable names without values"
  artifacts:
    - path: "src/index.css"
      provides: "All HSL CSS custom properties, glass utility classes, keyframes (breathe/pulse/incinerate/matrix-fall), :focus-visible globals"
      contains: "--accent-violet"
      min_lines: 120
    - path: "package.json"
      provides: "Vite + React 19 scaffold with firebase, lucide-react, @google/generative-ai pinned to researched versions"
      contains: "lucide-react"
    - path: "index.html"
      provides: "Vite entry point loading Outfit (600;700) + Inter (400;500) from Google Fonts with font-display:swap"
      contains: "Outfit"
    - path: ".env.example"
      provides: "Documented VITE_FIREBASE_* variable names with no values"
      contains: "VITE_FIREBASE_API_KEY"
    - path: ".gitignore"
      provides: "Ignores node_modules, dist, and .env* secrets"
      contains: ".env"
  key_links:
    - from: "index.html"
      to: "src/main.jsx"
      via: "script module entry"
      pattern: "main\\.jsx"
    - from: "src/main.jsx"
      to: "src/index.css"
      via: "global stylesheet import"
      pattern: "index\\.css"
---

<objective>
Scaffold the ZenStudy Vite + React 19 project and lay down the complete glassmorphic design system in `src/index.css` exactly as specified in DESIGN.md §1 and UI-SPEC. This is TRACK B foundation: it produces the runnable shell and the CSS contract every later component imports. No feature logic.

Purpose: Establishes ROADMAP success criterion 5 (design system renders correctly) and the credential-safe scaffold that makes mock mode possible (criterion 4 / AUTH-06). All Wave 2 component plans build against the CSS tokens defined here.
Output: A booting Vite app, `src/index.css` with all tokens/utilities/keyframes, font loading, and a gitignored credential setup with a committed `.env.example`.
</objective>

<execution_context>
@/Users/aquibvadsaria/project/playground/Mental-Wellness-Tracker/.claude/gsd-core/workflows/execute-plan.md
@/Users/aquibvadsaria/project/playground/Mental-Wellness-Tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@DESIGN.md
@IMPLEMENTATION_PLAN.md
</context>

<interface_context>
This plan produces the CSS contract consumed by every Wave 2 component. The exact identifiers later plans depend on:

- CSS custom properties (declared in `:root`): `--bg-deep`, `--bg-card`, `--border-glass`, `--border-glow`, `--accent-violet`, `--accent-teal`, `--accent-rose`, `--accent-orange`, `--text-main`, `--text-muted`, `--color-destructive`, `--font-display`, `--font-body`, `--glass-blur`.
- Spacing scale custom properties: `--space-xs:4px`, `--space-sm:8px`, `--space-md:16px`, `--space-lg:24px`, `--space-xl:32px`, `--space-2xl:48px`, `--space-3xl:64px`.
- Utility classes: `.glass-card`, `.btn-primary`, `.btn-secondary`.
- Keyframes: `breathe`, `pulse`, `incinerate`, `matrix-fall`.
- Global rule: `:focus-visible { outline: 2px solid var(--accent-violet); outline-offset: 3px; }`.
- Env var names: `VITE_FIREBASE_API_KEY`, `VITE_FIREBASE_AUTH_DOMAIN`, `VITE_FIREBASE_PROJECT_ID`, `VITE_FIREBASE_STORAGE_BUCKET`, `VITE_FIREBASE_MESSAGING_SENDER_ID`, `VITE_FIREBASE_APP_ID`, `VITE_GEMINI_API_KEY`.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React project with pinned dependencies and credential-safe config</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Standard Stack table — exact versions; Recommended Project Structure; Pitfall 7 on .env.example)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-01 JS/.jsx, D-02 deps, D-10 no committed keys)
    - IMPLEMENTATION_PLAN.md §2 Phase 1 (target files)
  </read_first>
  <files>package.json, vite.config.js, index.html, .gitignore, .env.example, src/main.jsx, src/App.jsx</files>
  <action>
    Scaffold a Vite + React project in the repo root (this directory already contains .planning/ and design docs; do NOT create a nested zenstudy/ subfolder — scaffold in place). Use the React (JavaScript, .jsx) template per D-01. Pin these exact versions from RESEARCH.md Standard Stack: vite 8.0.16, react 19.2.7, react-dom 19.2.7, @vitejs/plugin-react 6.0.2, firebase 12.14.0, lucide-react 1.18.0, @google/generative-ai 0.24.1 (per D-02 — no other runtime deps). Configure vite.config.js with defineConfig and the react() plugin.

    Write index.html as the Vite entry: a root div with id "root", a module script importing /src/main.jsx, and a Google Fonts link loading Outfit weights 600;700 and Inter weights 400;500 with font-display:swap (per UI-SPEC Design System row and RESEARCH Open Question 2 — include a fallback so layout is correct if fonts fail). Title: "ZenStudy".

    Write .gitignore to exclude node_modules, dist, and every .env variant (.env, .env.local, .env.*.local) per D-10. Write a committed .env.example documenting the variable NAMES with empty/placeholder values only (never real keys): VITE_FIREBASE_API_KEY, VITE_FIREBASE_AUTH_DOMAIN, VITE_FIREBASE_PROJECT_ID, VITE_FIREBASE_STORAGE_BUCKET, VITE_FIREBASE_MESSAGING_SENDER_ID, VITE_FIREBASE_APP_ID, VITE_GEMINI_API_KEY. Add a comment that leaving these blank runs the app in mock mode (AUTH-06 / SET-02). This directly addresses RESEARCH Pitfall 7.

    Write a minimal src/main.jsx that imports './index.css', creates the React root, and renders &lt;App /&gt; in StrictMode. Write a placeholder src/App.jsx that returns a single &lt;div className="loading-screen" /&gt; (the real state machine arrives in plan 01-05 — this stub only makes the scaffold boot). Do NOT install or reference react-router-dom (D-04). Verify the local Node version satisfies Vite 8's Node >= 20.19 requirement before running install (RESEARCH Environment Availability).
  </action>
  <verify>
    <automated>node --version &amp;&amp; npm install &amp;&amp; npm run build 2>&amp;1 | tail -5</automated>
    <human-check>Run `npm run dev`; confirm the dev server starts on http://localhost:5173 with no console errors and no Firebase/Gemini credentials present.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm install` succeeds and `npm run build` exits 0 with no credentials configured.
    - package.json lists exactly the seven pinned dependencies/devDependencies at the researched versions; react-router-dom and Tailwind are absent.
    - `git check-ignore .env.local` reports it is ignored; `.env.example` is committed and contains only variable names, no secret values.
    - index.html loads Outfit 600;700 and Inter 400;500 with font-display:swap.
  </acceptance_criteria>
  <done>The project boots and builds cleanly from a clean checkout with zero credentials, fonts load, and no secret material is tracked by git.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the complete glassmorphic design system in src/index.css</name>
  <read_first>
    - DESIGN.md §1 (CSS Theme Tokens block — verbatim HSL values; Aesthetic Guidelines)
    - .planning/phases/01-foundation/01-UI-SPEC.md (Color table, Typography, Spacing Scale, Animation Catalog, Accessibility Contract)
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 5 CSS Design System; Pitfall 6 backdrop-filter prefix)
  </read_first>
  <files>src/index.css</files>
  <action>
    Replace the scaffold-generated src/index.css entirely. Declare in :root every token from UI-SPEC Color table using the exact raw values from DESIGN.md §1: --bg-deep hsl(224,32%,8%), --bg-card hsla(224,30%,14%,0.6), --border-glass rgba(255,255,255,0.08), --border-glow rgba(139,92,246,0.15), --accent-violet hsl(263,90%,66%), --accent-teal hsl(162,76%,41%), --accent-rose hsl(342,75%,62%), --accent-orange hsl(24,94%,50%), --text-main hsl(210,40%,98%), --text-muted hsl(215,20%,65%), --color-destructive hsl(0,72%,51%), --font-display 'Outfit',sans-serif, --font-body 'Inter',sans-serif, --glass-blur blur(14px). Also declare the spacing scale tokens (--space-xs 4px through --space-3xl 64px) from the UI-SPEC Spacing Scale table.

    Set body/html background to var(--bg-deep), color var(--text-main), font-family var(--font-body), margin 0, min-height 100vh. Define a global *:focus-visible rule: outline 2px solid var(--accent-violet); outline-offset 3px (UI-SPEC Accessibility Contract — never use outline:none without a visible replacement).

    Implement the .glass-card utility per UI-SPEC Glassmorphic surface contract: background var(--bg-card); both backdrop-filter and -webkit-backdrop-filter set to var(--glass-blur) (Pitfall 6 — always pair the -webkit- prefix); border 1px solid var(--border-glass); border-radius 16px. Add the frosted-edge ::before overlay using linear-gradient(135deg, rgba(255,255,255,0.1), rgba(255,255,255,0.02)) per DESIGN.md §1.

    Implement .btn-primary (Outfit display font, background var(--accent-violet), color var(--text-main), border-radius 12px, padding 12px 24px, min-height 44px, cursor pointer, transition transform/box-shadow 0.1s) with :active { transform: translateY(2px) scale(0.98); } per DESIGN.md tactile-button guideline. Implement .btn-secondary (transparent background, 1px var(--border-glass) border, var(--text-muted) color, same size/active transform; hover shifts border to var(--border-glow) and color to var(--text-main)) for the Guest button.

    Define all four keyframes exactly from the UI-SPEC Animation Catalog: breathe (scale 1->1.08->1, opacity 0.8->1), pulse (opacity 0.6->1), incinerate (translateY 0->60px, scale 1->0.6, blur), matrix-fall (translateY 0->100px, letter-spacing->4px). incinerate and matrix-fall are defined now but applied in Phase 2 — define, do not apply. Add a .loading-screen class (full-viewport var(--bg-deep)) so the App stub renders cleanly. Use only token references — zero hardcoded hex/rgb/hsl literals outside :root (UI-SPEC HSL-tokens-only constraint). Never add a rule that animates backdrop-filter (UI-SPEC animation rule).
  </action>
  <verify>
    <automated>grep -q -- "--accent-violet: hsl(263, 90%, 66%)" src/index.css &amp;&amp; grep -q "@keyframes breathe" src/index.css &amp;&amp; grep -q "@keyframes incinerate" src/index.css &amp;&amp; grep -q "@keyframes matrix-fall" src/index.css &amp;&amp; grep -q -- "-webkit-backdrop-filter" src/index.css &amp;&amp; grep -q "focus-visible" src/index.css &amp;&amp; echo CSS_OK</automated>
    <human-check>Temporarily render a .glass-card on the page; confirm the frosted blur is visible (including Safari via -webkit- prefix) over the deep slate background and the violet focus ring appears on keyboard Tab.</human-check>
  </verify>
  <acceptance_criteria>
    - Every UI-SPEC Color-table token exists in :root with the exact DESIGN.md §1 raw value.
    - All four keyframes (breathe, pulse, incinerate, matrix-fall) are defined; incinerate and matrix-fall are not applied to any element in Phase 1.
    - Every glass surface pairs -webkit-backdrop-filter with backdrop-filter; no rule animates backdrop-filter.
    - A global :focus-visible outline is present; no component-targeted hex/rgb/hsl literals exist outside :root.
  </acceptance_criteria>
  <done>src/index.css is the single source of all visual tokens, utilities, and keyframes, matching DESIGN.md §1 and the UI-SPEC exactly, ready for Wave 2 components to consume by class name only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → git repository | Local `.env.local` secrets must never cross into committed files |
| build-time env → client bundle | Vite inlines `import.meta.env.VITE_*` into shipped JS — only non-secret config may live there |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env / git | mitigate | `.gitignore` excludes all `.env*`; only `.env.example` (names, no values) is committed (D-10). |
| T-01-02 | Information Disclosure | Vite client bundle | mitigate | Document in `.env.example` that the Gemini API key is NOT placed in a VITE_ var bundled to client; live Gemini wiring deferred to Phase 4 Settings panel (RESEARCH Security Domain). |
| T-01-03 | Tampering | npm install (vite, react, react-dom, @vitejs/plugin-react, firebase, lucide-react, @google/generative-ai) | accept | RESEARCH Package Legitimacy Audit verified all six as official-org packages (7M–138M downloads/wk, no postinstall scripts); SUS flags are false positives of the recency heuristic. No [ASSUMED]/[SUS]/[SLOP] residual — no blocking checkpoint required. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck satisfied by RESEARCH audit table; no unresolved [ASSUMED]/[SUS] packages, so no blocking human checkpoint is inserted. |
</threat_model>

<verification>
- `npm install && npm run build` exits 0 with zero credentials configured.
- `git check-ignore .env.local` confirms env secrets are ignored; `.env.example` committed with names only.
- src/index.css contains all tokens, utilities, keyframes per the grep gate above.
- Dev server boots at http://localhost:5173 showing the loading-screen stub with no console errors.
</verification>

<success_criteria>
- ROADMAP criterion 5 foundation: the glassmorphic design system (HSL tokens, keyframes, Outfit/Inter) is globally available and renders correctly.
- AUTH-06 foundation: the scaffold boots and builds with no Firebase/Gemini credentials present.
- No secret material is committed; `.env.example` documents the contract for live mode.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done.
</output>
