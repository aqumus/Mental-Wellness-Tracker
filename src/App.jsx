// Placeholder App component — the real state machine (auth → onboarding → dashboard)
// arrives in plan 01-05. This stub makes the scaffold boot cleanly.
export default function App() {
  return <div className="loading-screen" />;
}
