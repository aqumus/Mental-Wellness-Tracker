---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - src/App.jsx
  - src/main.jsx
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06]
must_haves:
  truths:
    - "App routes Auth → Onboarding → Dashboard purely from (user, profile) state with no react-router-dom"
    - "On page load a signed-in user is restored without a flash of the Auth screen, and is sent to Dashboard if their profile is complete or Onboarding if not"
    - "Guest entry reaches Onboarding in one click and a completed onboarding lands on the Dashboard, in both live and mock mode"
    - "The whole flow runs end-to-end with no Firebase or Gemini credentials configured"
  artifacts:
    - path: "src/App.jsx"
      provides: "The (user, profile) state machine: onAuthStateChanged in live mode, localStorage session in mock mode, conditional render of Auth/Onboarding/Dashboard with a loading guard"
      contains: "onAuthStateChanged"
      min_lines: 50
    - path: "src/main.jsx"
      provides: "React root rendering <App /> with global index.css import"
      contains: "createRoot"
  key_links:
    - from: "src/App.jsx"
      to: "src/services/firebase.js"
      via: "onAuthStateChanged(auth) live / MOCK_MODE localStorage branch"
      pattern: "onAuthStateChanged"
    - from: "src/App.jsx"
      to: "src/services/dbService.js"
      via: "getUser determines profileComplete"
      pattern: "getUser"
    - from: "src/App.jsx"
      to: "src/components/Auth.jsx"
      via: "renders <Auth onAuth=...> when no user"
      pattern: "Auth"
    - from: "src/App.jsx"
      to: "src/components/Onboarding.jsx"
      via: "renders <Onboarding> when user but no profile"
      pattern: "Onboarding"
    - from: "src/App.jsx"
      to: "src/components/Dashboard.jsx"
      via: "renders <Dashboard> when user + complete profile"
      pattern: "Dashboard"
---

<objective>
Wire everything together: replace the stub `App.jsx` with the (user, profile) state machine that routes Auth → Onboarding → Dashboard, and finalize `main.jsx` to render it. This integrates the service layer (plan 01-02) with the Auth/Onboarding (plan 01-03) and Navbar/Dashboard (plan 01-04) components into the complete Phase 1 entry loop.

Purpose: Closes ROADMAP success criteria 1, 2, 3, and 4 end-to-end and exercises all seven Phase 1 requirements (AUTH-01..06) as one flow. This is the only plan that touches App.jsx, so it runs after both Wave 2 component plans land.
Output: A working app where a brand-new visitor with zero credentials can sign up / log in (persisted) / enter as guest, complete onboarding, and land on a navigable dashboard.
</objective>

<execution_context>
@/Users/aquibvadsaria/project/playground/Mental-Wellness-Tracker/.claude/gsd-core/workflows/execute-plan.md
@/Users/aquibvadsaria/project/playground/Mental-Wellness-Tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@DESIGN.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<interface_context>
Integrates the contracts from all prior Phase 1 plans:
- From src/services/firebase.js: `auth`, `MOCK_MODE`.
- From firebase/auth (live mode): `onAuthStateChanged`.
- From src/services/dbService.js: `getUser`.
- From components: `Auth({ onAuth })`, `Onboarding({ user, onComplete })`, `Dashboard({ user, profile, onProfileUpdate })`.
- Mock-mode session key (written by Auth.jsx): `zenstudy:mock:user`.

App.jsx state contract: `user` initialized to `undefined` (loading), becomes `null` (signed out) or a user object; `profile` is the dbService user record or null. `profileComplete` is derived from `!!profile?.name`.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the App.jsx state machine and finalize main.jsx</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3 App.jsx state machine; Code Example onAuthStateChanged in App.jsx complete; Pitfall 1 loading state with user=undefined; Anti-Patterns: observer-only, no auth.currentUser on load)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-04 state-machine routing, no react-router-dom; D-07 guest temp profile then onboarding)
    - DESIGN.md §2.1 (auth-state diagram: New User → Onboarding, Returning → Dashboard)
    - .planning/phases/01-foundation/01-03-SUMMARY.md and 01-04-SUMMARY.md (final component prop signatures)
  </read_first>
  <files>src/App.jsx, src/main.jsx</files>
  <action>
    Replace the stub src/App.jsx with the state machine per RESEARCH Pattern 3 / Code Example. State: user initialized to undefined (loading sentinel — Pitfall 1), profile initialized to null. In a single useEffect:
    - Mock mode (MOCK_MODE true): read `zenstudy:mock:user` from localStorage; setUser to the parsed object or null; if present, call getUser(uid).then(setProfile). No observer/cleanup needed.
    - Live mode: subscribe with onAuthStateChanged(auth, cb); on each callback setUser(firebaseUser ?? null) and, if a user exists, await getUser(uid) then setProfile (else setProfile(null)). Return the unsubscribe function for cleanup. Never read auth.currentUser on load (Anti-Pattern) — the observer is the single source of truth.

    Render logic (D-04 conditional render, zero react-router-dom):
    - user === undefined → render &lt;div className="loading-screen" /&gt; (prevents the Auth flash, Pitfall 1).
    - user === null → render &lt;Auth onAuth={(u, p) => { setUser(u); setProfile(p); }} /&gt; (mock mode supplies u/p directly since there is no observer; live mode also fine because the observer will reconcile).
    - user && !profile?.name → render &lt;Onboarding user={user} onComplete={(p) => setProfile(p)} /&gt; (covers new email users AND guests per D-07 — guest has a user but no profile, so onboarding shows).
    - user && profile?.name → render &lt;Dashboard user={user} profile={profile} onProfileUpdate={setProfile} /&gt;.

    Finalize src/main.jsx: import './index.css', create the React root on #root, render &lt;App /&gt; inside &lt;StrictMode&gt;. Ensure no dangerouslySetInnerHTML anywhere and no inline `style={{}}` props (styling stays in Tailwind v4 utilities + the semantic classes from index.css). Confirm react-router-dom is neither imported nor in package.json (D-04).
  </action>
  <verify>
    <automated>grep -q "onAuthStateChanged" src/App.jsx &amp;&amp; grep -q "useState(undefined)" src/App.jsx &amp;&amp; grep -q "MOCK_MODE" src/App.jsx &amp;&amp; grep -q "Onboarding" src/App.jsx &amp;&amp; grep -q "Dashboard" src/App.jsx &amp;&amp; grep -q "createRoot" src/main.jsx &amp;&amp; ! grep -rq "react-router" src/ &amp;&amp; ! grep -rq "dangerouslySetInnerHTML" src/ &amp;&amp; npm run build 2>&amp;1 | tail -3</automated>
    <human-check>End-to-end in mock mode (no credentials): (1) sign up → onboarding → dashboard; refresh → land back on dashboard (AUTH-01/02/05). (2) Sign in path recognizes the persisted session (AUTH-02). (3) "Enter as Guest" → onboarding in one click → dashboard (AUTH-03). (4) Confirm no Auth-screen flash on reload for a logged-in user (Pitfall 1). (5) Confirm the entire flow works with no .env file present (AUTH-06).</human-check>
  </verify>
  <acceptance_criteria>
    - App routes solely on (user, profile) state; react-router-dom is absent from src/ and package.json.
    - user is initialized to undefined and a loading screen renders until auth resolves — no Auth flash for restored sessions.
    - New/guest users see Onboarding; users with a complete profile see Dashboard; both live (onAuthStateChanged) and mock (localStorage) paths work.
    - The full Auth→Onboarding→Dashboard loop completes with zero credentials; no dangerouslySetInnerHTML or inline `style={{}}` props in src/.
  </acceptance_criteria>
  <done>App.jsx integrates services and components into the complete Phase 1 entry loop, satisfying AUTH-01..06 end-to-end in both live and mock mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Firebase Auth observer → app state | Auth state is trusted only via the onAuthStateChanged observer |
| mock-mode localStorage session → app state | `zenstudy:mock:user` reconstructs the session on load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | Spoofing / Session | App.jsx auth-state source | mitigate | Derive auth state only from onAuthStateChanged in live mode (never auth.currentUser on load); mock session reads a uid-only object — no privilege claims (RESEARCH Anti-Patterns). |
| T-01-18 | Denial of Service | App.jsx initial render | mitigate | user=undefined loading guard prevents redirect loops / Auth flash (Pitfall 1). |
| T-01-19 | Tampering | observer cleanup | mitigate | Return the onAuthStateChanged unsubscribe from useEffect to avoid duplicate listeners across HMR/unmount. |
| T-01-20 | Tampering / XSS | end-to-end render path | mitigate | Repo-wide assertion that no dangerouslySetInnerHTML and no react-router exist in src/ (grep gate). |
</threat_model>

<verification>
- `npm run build` exits 0; grep gate confirms the state machine, observer, mock branch, component wiring, and absence of react-router/dangerouslySetInnerHTML.
- End-to-end manual matrix (mock mode, no credentials): signup→onboarding→dashboard with refresh persistence; login recognition; one-click guest→onboarding→dashboard; no Auth flash on reload.
- The full flow runs with no .env file present (AUTH-06).
</verification>

<success_criteria>
- ROADMAP criteria 1, 2, 3, 4 all demonstrably TRUE end-to-end.
- AUTH-01..06 exercised as a single integrated flow in both live and mock mode.
- Routing is pure (user, profile) state — no react-router-dom (D-04).
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` when done.
</output>
</content>
