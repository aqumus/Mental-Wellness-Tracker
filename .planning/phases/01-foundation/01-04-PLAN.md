---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/components/Navbar.jsx
  - src/components/Dashboard.jsx
autonomous: true
requirements: [AUTH-05, AUTH-06]
must_haves:
  truths:
    - "The dashboard shell is fully navigable: every feature tab (Vent, Garden, League, Bazaar, Insights) is present and switches the visible placeholder panel"
    - "The persistent Navbar shows the app name, the user's first name, a streak counter, and a Zen Point balance read from the profile (zero/initial values acceptable)"
    - "When MOCK_MODE is true a 'Demo mode — data stored locally.' banner renders on the dashboard"
  artifacts:
    - path: "src/components/Navbar.jsx"
      provides: "Fixed 56px frosted header: name, state-driven feature tabs, greeting/streak/ZP badges with ARIA"
      contains: "ZenStudy"
      min_lines: 60
    - path: "src/components/Dashboard.jsx"
      provides: "Navigable shell: time-aware greeting + one placeholder panel per feature tab + mock-mode banner"
      contains: "Your wellness journey continues."
      min_lines: 70
  key_links:
    - from: "src/components/Dashboard.jsx"
      to: "src/components/Navbar.jsx"
      via: "renders Navbar with active-tab state and tab-change handler"
      pattern: "Navbar"
    - from: "src/components/Navbar.jsx"
      to: "lucide-react"
      via: "streak/ZP badge icons"
      pattern: "lucide-react"
    - from: "src/components/Dashboard.jsx"
      to: "src/services/firebase.js"
      via: "reads MOCK_MODE to show the demo banner"
      pattern: "MOCK_MODE"
---

<objective>
Build the navigable dashboard shell (TRACK B components): `Navbar.jsx` (persistent frosted header with feature tabs and profile badges) and `Dashboard.jsx` (time-aware greeting, one placeholder panel per feature area, and the mock-mode banner). Per D-09 this is a navigable shell, not a bare page — but it implements no feature logic.

Purpose: Satisfies the "fully navigable" half of ROADMAP success criteria 2/3/4 and the AUTH-05 landing target. Placeholders are replaced as Phases 2–4 land features. Built in parallel with Auth/Onboarding (plan 01-03) — no shared files.
Output: Two components rendering the exact UI-SPEC Navbar and Dashboard surfaces and copywriting, with state-driven tab switching and accessible nav.
</objective>

<execution_context>
@/Users/aquibvadsaria/project/playground/Mental-Wellness-Tracker/.claude/gsd-core/workflows/execute-plan.md
@/Users/aquibvadsaria/project/playground/Mental-Wellness-Tracker/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@DESIGN.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<interface_context>
Consumes contracts from plan 01-01 (CSS) and plan 01-02 (services):
- From src/index.css: the semantic component class `.glass-card`, all `--*` design tokens (declared via Tailwind v4 `@theme`), the frosted-nav styling, --accent-violet active-tab indicator. Tailwind v4 utility classes are also available directly in JSX.
- From src/services/firebase.js: `MOCK_MODE` (drives the demo banner).
- From lucide-react: a flame/streak icon and a star/ZP icon (per UI-SPEC Navbar right slot).

Props this plan EXPORTS for the App.jsx wiring (plan 01-05):
- `Dashboard({ user, profile, onProfileUpdate })` — top-level shell; owns the active-tab state internally.
- `Navbar({ profile, activeTab, onTabChange })` — receives active tab + change handler from Dashboard; renders name/streak/ZP from `profile` (tolerating undefined → 0/initial values).

The five tab ids are fixed: `vent`, `garden`, `league`, `bazaar`, `insights`.
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Build Navbar.jsx — fixed frosted header with accessible feature tabs and profile badges</name>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (Surface 3: Navbar — 56px height, left/center/right slots, tab list, ARIA contract role=tablist/tab/aria-selected/aria-current, 44px targets; Copywriting → Navbar exact tab labels and aria-label tooltips; Implementation Constraints → Tailwind v4 styling, no inline styles)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-09 navbar reads name/streak/ZP from profile, initial values fine; tabs inert in Phase 1 — click logs console.info in dev)
    - IMPLEMENTATION_PLAN.md §2 Phase 3 (Navbar shows streak counter, name, points)
  </read_first>
  <files>src/components/Navbar.jsx</files>
  <action>
    Create src/components/Navbar.jsx as a function component accepting { profile, activeTab, onTabChange }. Render a semantic &lt;header role="banner"&gt; with the UI-SPEC Surface 3 layout: fixed top, full width, 56px height, frosted nav styling (background --bg-card + paired backdrop-filter/-webkit-backdrop-filter + bottom 1px --border-glass border), via Tailwind v4 utility classes and/or the semantic component classes from src/index.css (no inline `style={{}}` props).

    Left slot: app name "ZenStudy" in Outfit 20px weight 600. Center slot: a &lt;nav aria-label="Main navigation"&gt; containing a role="tablist" of five &lt;button role="tab"&gt; tabs with the exact labels Vent · Garden · League · Bazaar · Insights mapped to ids vent/garden/league/bazaar/insights. The active tab gets the --accent-violet 2px underline indicator, aria-selected="true" and aria-current="page"; inactive tabs are --text-muted Inter 14px weight 500. Each tab is ≥44px wide and keyboard-activatable (Enter/Space). Clicking a tab calls onTabChange(id); additionally console.info the tab id when import.meta.env.DEV (D-09 — inert placeholders in Phase 1).

    Right slot: greeting snippet (profile.name first name, Outfit 14px weight 500) + streak badge (lucide-react flame icon + (profile.streak ?? 0), --accent-violet text, aria-label "[n]-day streak") + ZP badge (lucide-react star icon + (profile.zenPoints ?? 0), --text-muted text, aria-label "[n] Zen Points"). Tolerate a missing/partial profile by defaulting to 0/empty. Render all dynamic values as React text nodes (no dangerouslySetInnerHTML). Keep the badge tooltip text as aria-label only (not visible text) per the Copywriting Contract.
  </action>
  <verify>
    <automated>grep -q "ZenStudy" src/components/Navbar.jsx &amp;&amp; grep -q "role=\"tablist\"" src/components/Navbar.jsx &amp;&amp; grep -q "aria-current" src/components/Navbar.jsx &amp;&amp; grep -q "lucide-react" src/components/Navbar.jsx &amp;&amp; grep -q "onTabChange" src/components/Navbar.jsx &amp;&amp; grep -Lq "dangerouslySetInnerHTML" src/components/Navbar.jsx &amp;&amp; npm run build 2>&amp;1 | tail -3</automated>
    <human-check>Confirm the frosted 56px header renders all five tabs with the violet active underline, keyboard Tab cycles the tabs and Enter activates them, and the right slot shows name + streak + ZP from a sample profile (0 values acceptable).</human-check>
  </verify>
  <acceptance_criteria>
    - Header is semantic (role="banner" with a nav/tablist), all five tabs present with exact labels, active tab has the violet underline + aria-selected/aria-current.
    - Tabs are ≥44px and keyboard-operable; onTabChange fires with the correct id; dev clicks log console.info.
    - Name/streak/ZP read from profile with safe defaults; badge tooltips are aria-label only; no inline `style={{}}` props (Tailwind utilities + semantic classes only); no dangerouslySetInnerHTML.
  </acceptance_criteria>
  <done>Navbar.jsx is the persistent, accessible header that drives state-machine tab navigation and surfaces the profile snapshot, ready for Dashboard and App to wire.</done>
</task>

<task type="auto">
  <name>Task 2: Build Dashboard.jsx — time-aware greeting, placeholder panels, mock-mode banner</name>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (Surface 4: Dashboard Shell — layout offset, greeting section + time-of-day rules, 5 placeholder panels, active-panel conditional render, mock-mode indicator; Copywriting → Dashboard Shell exact greeting + panel + banner strings; Implementation Constraints → Tailwind v4 styling, no inline styles)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-09 navigable shell with placeholder regions; no feature logic)
    - .planning/phases/01-foundation/01-02-SUMMARY.md (MOCK_MODE export from firebase.js)
  </read_first>
  <files>src/components/Dashboard.jsx</files>
  <action>
    Create src/components/Dashboard.jsx as a function component accepting { user, profile, onProfileUpdate }. Own an activeTab state (default 'vent') and render Navbar with { profile, activeTab, onTabChange: setActiveTab }. Lay out the content area below the fixed 56px navbar per UI-SPEC Surface 4 (56px top offset, max-width 1024px centered, 24px padding) on a --bg-deep base, in a semantic &lt;main&gt;, using Tailwind v4 utility classes and/or semantic component classes (no inline `style={{}}` props).

    Greeting section: Outfit 28px weight 700 greeting using the exact time-of-day strings — "Good morning, [firstName]." (local hour 5–11), "Good afternoon, [firstName]." (12–16), "Good evening, [firstName]." (17–20), "Hello, [firstName]." (21–4) — derive firstName from profile.name; Inter 16px weight 400 subtitle "Your wellness journey continues." Render the name as a React text node.

    Define the five placeholder panels (vent/garden/league/bazaar/insights), each a .glass-card with 24px padding, an Outfit 20px weight 600 title and an Inter 16px weight 400 body, using the exact Dashboard-Shell copywriting strings (e.g., Vent Rooms / "Four themed rooms to release stress. Coming soon.", etc.). Conditionally render ONLY the panel matching activeTab (not CSS display:none) per UI-SPEC — keeps the DOM clean and proves navigability. No CTA buttons in placeholders (replaced in Phases 2–4).

    Mock-mode banner: import MOCK_MODE from firebase.js; when true render a small pill banner at the top of the dashboard with the exact text "Demo mode — data stored locally." styled per UI-SPEC (teal-tinted pill) via Tailwind utilities / semantic classes. No inline `style={{}}` props; no dangerouslySetInnerHTML. onProfileUpdate is accepted for forward compatibility (Phases 2+ will call it) but Phase 1 makes no profile mutations here.
  </action>
  <verify>
    <automated>grep -q "Your wellness journey continues." src/components/Dashboard.jsx &amp;&amp; grep -q "Demo mode — data stored locally." src/components/Dashboard.jsx &amp;&amp; grep -q "Four themed rooms to release stress. Coming soon." src/components/Dashboard.jsx &amp;&amp; grep -q "MOCK_MODE" src/components/Dashboard.jsx &amp;&amp; grep -q "Navbar" src/components/Dashboard.jsx &amp;&amp; grep -Lq "dangerouslySetInnerHTML" src/components/Dashboard.jsx &amp;&amp; npm run build 2>&amp;1 | tail -3</automated>
    <human-check>Confirm the greeting matches the current time-of-day with the user's first name, clicking each navbar tab swaps the visible placeholder panel (only one mounted at a time), and the "Demo mode — data stored locally." banner appears in mock mode.</human-check>
  </verify>
  <acceptance_criteria>
    - Greeting uses the correct time-of-day string and the user's first name; subtitle copy is exact.
    - All five placeholder panels carry the exact UI-SPEC titles/bodies; only the active panel is mounted; switching tabs changes it.
    - The mock-mode banner renders with exact copy when MOCK_MODE is true; no inline `style={{}}` props (Tailwind utilities + semantic classes only); no dangerouslySetInnerHTML.
  </acceptance_criteria>
  <done>Dashboard.jsx is a fully navigable shell — greeting, tab-driven placeholder panels, and the demo banner — providing the AUTH-05 landing target without feature logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile data → rendered DOM | profile.name and counters are rendered into the navbar/greeting |
| mock-mode flag → UI | MOCK_MODE gates a visible demo indicator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Tampering / XSS | Navbar/Dashboard rendering profile.name | mitigate | Render name and all dynamic values as React text nodes; no dangerouslySetInnerHTML (CLAUDE.md / IMPLEMENTATION_PLAN.md). |
| T-01-15 | Information Disclosure | mock-mode banner | accept | Banner discloses only that data is local — intentional examiner UX, no sensitive data exposed. |
| T-01-16 | Repudiation / a11y | navbar tab semantics | mitigate | role=tablist/tab + aria-selected/aria-current + keyboard operability per UI-SPEC Accessibility Contract (supports the Phase 4 a11y audit criterion). |
</threat_model>

<verification>
- `npm run build` exits 0 with both components present.
- grep gates confirm verbatim copywriting, ARIA roles, and the Navbar↔Dashboard wiring identifiers.
- Manual: greeting matches time-of-day; tab switching swaps placeholder panels; demo banner shows in mock mode.
- No inline `style={{}}` props and no dangerouslySetInnerHTML in either component.
</verification>

<success_criteria>
- ROADMAP "fully navigable" requirement: the dashboard shell switches between all five feature placeholders via accessible tabs.
- AUTH-05 landing target ready: Dashboard renders the post-onboarding home with the profile snapshot.
- AUTH-06 surfaced: mock mode is visibly indicated; shell works with zero credentials.
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done.
</output>
</content>
